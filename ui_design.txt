<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Dashboard with Sidebar</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

<style>
body {
  background: linear-gradient(135deg, #eef2f7, #f8fbff);
  font-family: 'Poppins', sans-serif;
}

/* Sidebar */
.sidebar {
  width: 260px;
  height: 100vh;
  position: fixed;
  background: linear-gradient(180deg, #2C3E50, #34495E);
  color: white;
  top: 0;
  left: 0;
  padding-top: 20px;
  transition: 0.3s;
  z-index: 1000;
}

.sidebar h4 {
  text-align: center;
  margin-bottom: 30px;
}

.sidebar a {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 20px;
  color: #ddd;
  text-decoration: none;
  border-radius: 10px;
  margin: 5px 10px;
}

.sidebar a:hover,
.sidebar a.active {
  background: #18BC9C;
  color: white;
}

/* Hide sidebar on mobile */
@media(max-width:768px){
  .sidebar {
    left: -260px;
  }
  .sidebar.active {
    left: 0;
  }
}

/* Overlay */
.overlay {
  position: fixed;
  width: 100%;
  height: 100%;
  background: rgba(0,0,0,0.5);
  top: 0;
  left: 0;
  display: none;
  z-index: 900;
}

.overlay.active {
  display: block;
}

/* Main */
.main {
  margin-left: 260px;
  padding: 25px;
  transition: 0.3s;
}

@media(max-width:768px){
  .main {
    margin-left: 0;
  }
}

/* Topbar */
.topbar {
  background: rgba(255,255,255,0.7);
  backdrop-filter: blur(10px);
  padding: 15px 20px;
  border-radius: 15px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 25px;
}

/* Cards */
.card-box {
  padding: 20px;
  border-radius: 20px;
  color: white;
  position: relative;
}

.users { background: linear-gradient(135deg, #3498DB, #6DD5FA); }
.food { background: linear-gradient(135deg, #1ABC9C, #16A085); }
.requests { background: linear-gradient(135deg, #F39C12, #F1C40F); }
.delivery { background: linear-gradient(135deg, #E74C3C, #FF6B6B); }

/* Table */
.table-box {
  background: white;
  padding: 20px;
  border-radius: 20px;
  margin-top: 20px;
}
</style>
</head>

<body>

<!-- Sidebar -->
<div class="sidebar" id="sidebar">
  <h4>🍽 Food System</h4>
  <a class="active" href="#"><i class="fas fa-home"></i> Dashboard</a>
  <a href="#"><i class="fas fa-users"></i> Users</a>
  <a href="#"><i class="fas fa-utensils"></i> Food</a>
  <a href="#"><i class="fas fa-hand-holding"></i> Requests</a>
  <a href="#"><i class="fas fa-truck"></i> Delivery</a>
  <a href="#"><i class="fas fa-sign-out-alt"></i> Logout</a>
</div>

<!-- Overlay -->
<div class="overlay" id="overlay" onclick="toggleSidebar()"></div>

<!-- Main -->
<div class="main">

  <!-- Topbar -->
  <div class="topbar">
    <button class="btn btn-dark d-md-none" onclick="toggleSidebar()">
      <i class="fas fa-bars"></i>
    </button>
    <h5>Dashboard</h5>
    <span>Admin 👋</span>
  </div>

  <!-- Cards -->
  <div class="row g-4">
    <div class="col-md-3 col-6">
      <div class="card-box users">
        <h6>Users</h6>
        <h3>120</h3>
      </div>
    </div>

    <div class="col-md-3 col-6">
      <div class="card-box food">
        <h6>Food</h6>
        <h3>75</h3>
      </div>
    </div>

    <div class="col-md-3 col-6">
      <div class="card-box requests">
        <h6>Requests</h6>
        <h3>45</h3>
      </div>
    </div>

    <div class="col-md-3 col-6">
      <div class="card-box delivery">
        <h6>Delivery</h6>
        <h3>30</h3>
      </div>
    </div>
  </div>

  <!-- Table -->
  <div class="table-box">
    <h6>Recent Requests</h6>
    <table class="table mt-3">
      <tr><th>ID</th><th>User</th><th>Status</th></tr>
      <tr><td>1</td><td>Rahim</td><td>Pending</td></tr>
    </table>
  </div>

</div>

<script>
function toggleSidebar() {
  document.getElementById("sidebar").classList.toggle("active");
  document.getElementById("overlay").classList.toggle("active");
}
</script>

</body>
</html>
